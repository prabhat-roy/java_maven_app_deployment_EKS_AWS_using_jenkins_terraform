apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "java-maven.fullname" . }}-test-connection"
  labels:
    {{- include "java-maven.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "java-maven.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
